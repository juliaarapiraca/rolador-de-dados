import React from 'react';
import ReactDOM from 'react-dom/client';

// search for better forms to insert styles, via css, themes or other ways
// inser main styles for fontFamily, fontSizes, and let it standarized

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <>Application Dice Roller</>
  </React.StrictMode>
);
